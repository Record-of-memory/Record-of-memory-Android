package com.recordOfMemory.src.daybook

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import com.recordOfMemory.config.BaseActivity
import com.recordOfMemory.databinding.ActivityDaybookBinding
import com.recordOfMemory.databinding.ActivityDaybookWritingBinding

class DaybookWritingActivity : BaseActivity<ActivityDaybookWritingBinding>(ActivityDaybookWritingBinding::inflate) {
	override fun onCreate(savedInstanceState: Bundle?) {
		super.onCreate(savedInstanceState)
	}
}